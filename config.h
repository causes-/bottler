char *host = NULL;       /* server hostname */
char *port = NULL;       /* server port */
char *nick = NULL;       /* bot nickname */
char *name = NULL;       /* bot username */
char *channel = NULL;    /* channel to join */
