#ifndef CONFIG_H
#define CONFIG_H

// server hostname
char *host = NULL;

// server port
char *port = NULL;

// bot nickname
char *nick = NULL;

// bot username
char *name = NULL;

// bot owner
char *owner = NULL;

// autojoin channels
char *channels = NULL;

#endif
