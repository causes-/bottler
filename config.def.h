#ifndef CONFIG_H
#define CONFIG_H

// server hostname
char *host = NULL;

// server port
char *port = NULL;

// bot nickname
char *botnick = NULL;

// bot username
char *botname = NULL;

// bot owner
char *owner = NULL;

// autojoin channels
char *channels = NULL;

#endif
